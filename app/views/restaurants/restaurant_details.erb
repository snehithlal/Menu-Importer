<% flash[:notice] %>
<div>
  <h4> Import Data </h4>
  <%= link_to "Import", new_import_restaurant_path(@restaurant), class: 'ui button' %>
  <table class="ui collapsing table">
  <thead>
    <tr>
      <th>Name</th>
      <th>Status</th>
    </tr>
  </thead>
    <% @restaurant.menu_categories.each do |category|%>
      <tbody>
        <tr>
          <td><%= category.name %></td>
          <td><%= category.menu_items.count %></td>
        </tr>
      </tbody>
      <% end %>
</table>
</div>