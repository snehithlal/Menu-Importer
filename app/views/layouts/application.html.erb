<!DOCTYPE html>
<html>
  <head>
    <title>MenuImporter</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="ui grid container">
      <div class="row">
        <div class="column">
          <h1 class="ui header inverted red">Restaurant Manager</h1>
           <% if user_signed_in? %>
            <div class="ui right floated button"><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></div>
            <% end %>
        </div>
      </div>
      <%= yield %>
    </div>
  </body>
</html>
